# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "rtaudio-sys"
version = "0.3.6"
authors = ["Billy Messenger <billydm@noreply.codeberg.org>"]
build = "build.rs"
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Raw bindings for RtAudio"
readme = "README.md"
keywords = [
    "audio",
    "sound",
    "rtaudio",
]
categories = [
    "multimedia::audio",
    "external-ffi-bindings",
]
license = "MIT"
repository = "https://codeberg.org/Meadowlark/rtaudio-rs"

[features]
alsa = []
asio = []
coreaudio = []
default = [
    "coreaudio",
    "alsa",
    "pulse",
    "oss",
    "wasapi",
    "ds",
]
ds = []
jack_linux = []
oss = []
pulse = []
wasapi = []

[lib]
name = "rtaudio_sys"
path = "src/lib.rs"

[dependencies]

[build-dependencies.cmake]
version = "0.1"

[target.'cfg(any(target_os="linux", target_os = "freebsd", target_os = "openbsd", target_os = "netbsd"))'.build-dependencies.pkg-config]
version = "0.3"
